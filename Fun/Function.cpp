//
// Created by lfnnx on 21.06.19.
//

#include "Function.h"

unsigned char Function::evaluate(unsigned x, unsigned y) {
    unsigned toReturn = 0;

    for(order::iterator it = order1.begin(); it != order1.end(); ++it) {
        switch (*it){
            case l:
                break;
            case a:
                break;
            case v:
                break;
            default:
                break;
        }
    }

    return 0;
}
